Solucion punto 2 examen:

*La clase tiene 3 clases, la principal o main, animal y familia.
*clase zoologico, que le cambie el nombre a main
*se crean 2 objetos
*4 atributos, nombre, genero, peso y pareja.
*2, el atributo pareja y el peso.
* si eliminamos la linea 17 de la clase principal se ejecuta el programa normalmente.
*tiene 3 métodos
*tiene un parámetro.
*el constructor de Familia inicializa 2 atributos
*no retorna nada, es un método void.